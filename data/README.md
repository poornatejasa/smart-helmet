# Data

Raw and processed recordings are ignored by Git by default. Record an anonymous
subject/trial ID, firmware commit, board and sensor models, sampling rate, units,
conditions, labels, calibration, and warm-up procedure for every session.

Commit schemas and small anonymized examples, not private or identifying data.
