# Sensor Tests

Use one independently buildable directory per component:

```text
alcohol/
helmet-ir/
mpu6050/
vibration-motor/
```

Each component directory should contain source code plus a README covering its
wiring, dependencies, test procedure, and expected readings.
