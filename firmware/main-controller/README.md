# Main-controller Firmware

Keep known-good isolated programs in `sensor-tests/`. Add combined application
code to `app/` only after individual sensors work reliably. Rename this folder
to the exact STM32 target later if useful.
