# Inter-processor Communication

The STM32 and ESP32 are planned to communicate over UART. Before integration,
define the baud rate, framing, message types, field units, checksum, timeout,
retry behaviour, and protocol version here.
